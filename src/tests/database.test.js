const faker = require("faker");
const port = process.env.PORT || 3333;

describe('Database', () => {
  it('should create a search entry', () => {

  });

  it('should delete a search entry', () => {

  });

  it('should create a repository entry', () => {

  });

  it('should delete a repository entry', () => {

  });

});
